name: Deploy SimpleMessagingApp

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
      - name: create .env file
        run: echo "PORT=${{ secrets.PORT }}" >> .env
      - name: Login to docker hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - name: Build docker image
        run: docker build . -t ${{ secrets.DOCKER_USERNAME }}/simple-messaging-app
      - name: Push docker image to docker hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/simple-messaging-app
  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Pull docker image
        run: docker pull ${{ secrets.DOCKER_USERNAME }}/simple-messaging-app:latest
      - name: Delete old docker container
        run: docker rm -f simple-messaging-app
      - name: Run new docker container
        run: docker run -d -p 8080:8080 -p 4000:4000 --name simple-messaging-app ${{ secrets.DOCKER_USERNAME }}/simple-messaging-app